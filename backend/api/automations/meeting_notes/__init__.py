"""Meeting Notes automation package.

Currently exposes a thin, read-only test surface (routes.py + graph.py) for
verifying the app-only Microsoft Graph connection and that meeting transcripts
can be fetched. The full transcript->summary->email pipeline is not wired here.
"""
